name: Run pytest
on:
  push:
    branches: [master]


jobs:
  test-backend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - run: cp .env.example .env
      - run: make build
      - run: make test